{
  "name": "pnpm-11-repro-bug",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "preview": "pnpm --filter web preview"
  },
  "devDependencies": {
    "@code-obos/azure-env": "1.0.3"
  }
}